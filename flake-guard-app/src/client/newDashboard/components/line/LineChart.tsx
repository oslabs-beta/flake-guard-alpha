import React, {useEffect, useState} from 'react';
import {ResponsiveLine} from '@nivo/line';
import {lineChartParser} from '../../../utilities';



const LineChart: React.FC = () => {
  // console.log('results LineChart', results);
  // const [lineChartData, setLineChartData] = useState(null);

  // useEffect(() => {
  //   const dataPoints = lineChartParser(results);
  //   setLineChartData({id: 'flake', color: 'red', data: dataPoints});
  // }, [results]);

  return <div style={{height: '400px', width: '100%'}}>helloooo</div>;
};

export default LineChart;
